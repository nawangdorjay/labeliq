# dependencies
node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# env files — never commit secrets
.env
.env.*
!.env.example

# database — local SQLite is ignored, but the demo snapshot IS committed
# (serverless /tmp boot copy; see src/lib/db.ts)
db/*
*.db
!db/seed.db
*.db-journal

# misc
.DS_Store
*.pem
*.log

# vercel cli local config
.vercel/
